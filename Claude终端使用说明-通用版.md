# Claude Code 终端使用说明（通用版）

> 适用于任何文件夹、任何项目。照着做就行，不用理解原理。

---

## 核心流程（记住这五步就够了）

```
打开终端 → 进入文件夹 → 启动 claude → 干活 → 保存
```

每次都一样，只有「进入哪个文件夹」会变。

---

## 一、打开终端

按 `Command + 空格`，屏幕中间出现搜索框，输入 **终端**，按回车。

弹出的窗口里有一行字，最后是 `%` 符号，光标在闪。可以打字了。

---

## 二、进入你要改的文件夹（每次唯一变化的一步）

### 万能方法：拖拽

**不管什么文件夹都适用，永远不会打错字：**

1. 在终端里敲 `cd`，然后**打一个空格**（注意：别急着回车）
2. 打开访达，找到你要改的那个文件夹
3. 把文件夹**直接拖进终端窗口**
4. 完整路径自动出现
5. 按**回车**

**成功的标志**：窗口顶部标题变成了那个文件夹的名字，
提示符也从 `mac@192 ~ %` 变成 `mac@192 文件夹名 %`。

> 这一步是整套流程里唯一需要动脑的地方。
> Claude Code 只能看到「你当前所在的文件夹」，所以必须先站进去。

### 想确认自己站在哪

```
pwd
```

会打印出当前完整路径。

```
ls
```

会列出当前文件夹里有什么。看到熟悉的文件名就说明站对了。

---

## 三、先备份（强烈建议）

### 情况 A：这个文件夹是 git 项目

（不确定的话，跑 `ls -a` 看有没有 `.git` 这一项）

```
git add -A && git commit -m "backup"
```

出现 `nothing to commit` 也没关系，说明之前已经备份过了。

### 情况 B：不是 git 项目

那就手动复制一份：

```
cp -r . ../$(basename $PWD)-backup-$(date +%m%d)
```

这行会在上一层目录生成一个带日期的副本文件夹。

**或者更简单**：在访达里直接 `Command + C`、`Command + V` 复制整个文件夹。

> ⚠️ 没有备份就让 AI 改文件，等于闭着眼睛开车。
> 它改坏了你可能都不知道哪里坏了。

---

## 四、启动

```
claude
```

**成功的标志**：出现橙色欢迎框，左下角写着 `deepseek-v4-pro`，
底部有输入框，光标在闪。

第一次在新文件夹启动会问 `Do you trust the files in this folder?`
（信任这个文件夹里的文件吗）→ 选 `Yes, proceed`，回车。

---

## 五、干活

### 怎么下指令

底部输入框直接打字，中文就行，打完回车。

它会先说打算怎么做，然后请求读文件或改文件。
出现询问时用**方向键**选 `Yes`，按**回车**确认。

### 好指令的三个要素

**改哪个文件 + 想要什么效果 + 参照谁**

| ❌ 说不清 | ✅ 说得清 |
|---|---|
| 「改成粉色」 | 「照着 A.html 的配色改，用它的 CSS 变量」 |
| 「不太好看」 | 「纯白偏冷，跟暖调不搭，去看看 A.html 背景用的什么值」 |
| 「优化一下」 | 「把标题字体统一成正文的宋体，只改字体，别动颜色字号」 |

**参照物是最有用的东西**。指着一个已有的东西说「照这个来」，
比你自己描述十句都精确。

### 保命句式

在指令最后加一句：

```
先说方案，我确认后再改。
```

这样它不会一上来就动手，你有机会喊停。
DeepSeek 在多文件编辑上比较毛躁，这句话能省掉很多返工。

### 常用按键

| 按键 / 命令 | 作用 |
|---|---|
| `/clear` | 清空对话。**换任务时一定要用**，不然越来越费钱 |
| `/init` | 生成 CLAUDE.md 项目说明，以后不用重复解释项目结构 |
| `/status` | 查看当前用的什么模型、连的哪个服务器 |
| `Esc` | 打断它正在做的事 |
| `Esc` 按两下 | 回退到之前的对话节点 |
| `Ctrl + C` 按两下 | 退出 Claude Code |

**进入一个新项目时，建议先跑一次 `/init`**。
它会自动扫描项目、生成一份说明文件，以后每次对话自动加载。

---

## 六、干完活

### 第 1 步：退出

`Ctrl + C` 按两下，回到 `%` 提示符。

### 第 2 步：看效果

- **网页项目** → 访达里双击 `.html` 文件，用浏览器打开。
  再改再看的话，浏览器里按 `Command + R` 刷新。
- **其他文件** → 正常双击打开就行。

### 第 3 步：满意就保存

**git 项目：**

```
git add -A
```

```
git commit -m "这次改了什么"
```

如果这个项目要推到 GitHub，再加一行：

```
git push
```

引号里的字随便写，给自己看的备注，中文也行。

**非 git 项目**：文件已经直接改好了，不用额外操作。

### 第 4 步：不满意就还原

**git 项目：**

```
git checkout .
```

一秒钟回到备份那一刻，什么痕迹都不留。

**非 git 项目**：把之前复制的备份文件夹换回来。

---

## 七、常见问题

### 提示 `no such file or directory`

路径不对。别自己敲，用第二节的**拖拽方法**重来一次。

### 敲 `claude` 提示 `command not found`

先 `Command + Q` 彻底退出终端，重新打开再试。
还不行就跑 `ls -la ~/.npm-global/bin/claude` 看程序还在不在。

### 报错 `Failed to connect to api.anthropic.com`

DeepSeek 配置丢了或网络抽风。先重试一次，还不行就检查配置：

```
cat ~/.claude/settings.json
```

正常应该能看到 `api.deepseek.com` 字样。

### 怎么确认真的在用 DeepSeek

启动后看左下角写着 `deepseek-v4-pro` 就对了。
或在里面输入 `/status`，看 Base URL 是不是 `api.deepseek.com/anthropic`。

**最靠谱的验证**：去 `platform.deepseek.com` 看用量有没有在涨。
这个骗不了人。

### 文件明明在，它却说读不到

如果文件在 iCloud 里，可能被卸载到云端了。
访达里右键那个文件夹 → 「立即下载」，等一会儿再试。

> 顺带说：**iCloud 不太适合放代码项目**。
> 它会把不常用文件卸载成占位符，也可能同步坏 `.git` 文件夹。
> 有条件的话把项目放在 `~/Documents` 这类本地目录。

---

## 八、给常用项目做快捷方式（可选）

如果某个文件夹你经常要改，可以给它建个两字母的缩写。

**先在终端里跑一次**（`简称` 和路径换成你自己的）：

```
echo "alias 简称='cd 完整路径 && claude'" >> ~/.zshrc && source ~/.zshrc
```

比如：

```
echo "alias yy='cd ~/Documents/yystudy && claude'" >> ~/.zshrc && source ~/.zshrc
```

之后打开终端只要敲 `yy` 回车，直接进目录并启动。

**路径怎么填**：先用拖拽的办法 `cd` 进去，然后跑 `pwd`，
把打印出来的路径复制过来就行。

---

## 九、三条省钱习惯

1. **换任务就 `/clear`** —— 上下文越长，每次对话越贵
2. **新项目先 `/init`** —— 省掉每次重复解释项目结构
3. **别让它读大文件** —— 几 MB 的数据文件读一次就能烧掉平时几十次的量。
   需要时明确说「不要读 XX 这类大文件」

---

## 十、记住这三件事

1. **先备份再开工**
2. **指令里加「先说方案」**
3. **改坏了不慌，还原就行**

其他的都能查、能问、能重来。
